import { css } from 'styled-components';

export default css`
	display: flex;
	flex-direction: column;
	border: 1px solid;
	border-color: var(--borders);

	.week {
		display: flex;
		margin: 0 auto;
	}
`;
