import React from 'react';

const Recommendation = () => {
	return (
		<div>
			Tolerance to hot weather ...
			<button>X</button>
			<button>~</button>
			<button>√</button>
		</div>
	);
};

export default Recommendation;
